// ignore_for_file: public_member_api_docs

import 'package:flutter/material.dart';

const sentinel = ' ';
const sentinelValue = TextEditingValue(
  text: sentinel,
  selection: TextSelection.collapsed(offset: sentinel.length),
);
